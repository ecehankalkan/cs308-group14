$kind: http-request
name: Delete Cart Item
method: DELETE
url: '{{baseUrl}}/api/cart/{{cartItemId}}/'
order: 5000
description: Remove an item from the cart.
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{accessToken}}'
