$kind: http-request
name: Get Cart
method: GET
url: '{{baseUrl}}/api/cart/'
order: 1000
description: Get all cart items for the authenticated user.
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{accessToken}}'
